- name: test
  hosts: localhost
  gather_facts: false

  tasks:
    - name: Run pytest and capture output
      command: "{{ project_root }}/venv/bin/pytest tests/ --tb=short -q"
      register: pytest_output

    - name: Show pytest results
      debug:
        var: pytest_output.stdout_lines
